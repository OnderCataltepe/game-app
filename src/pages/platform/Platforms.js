import { CategoryLayout } from 'layouts';
import { platformsImg } from 'assets';
const Platforms = () => {
  return <CategoryLayout title="Choose Your Platform" bgImage={platformsImg} type="platforms" />;
};

export default Platforms;
